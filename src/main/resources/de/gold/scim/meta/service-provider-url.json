{
  "schemas": [
    "urn:ietf:params:scim:schemas:core:2.0:Schema"
  ],
  "id": "urn:gold:params:scim:schemas:extension:url:2.0:ServiceProvider",
  "name": "ServiceProviderUrl",
  "description": "An extension that holds a fallback base URL to build location attributes which is used if no other ways are provided to build a location url",
  "attributes": [
    {
      "name": "baseUrl",
      "type": "reference",
      "description": "A uri that provides the base path to the SCIM endpoints e.g. https://localhost/scim/v2",
      "mutability": "readOnly",
      "returned": "always",
      "uniqueness": "none",
      "multiValued": false,
      "caseExact": true,
      "required": true
    }
  ]
}
